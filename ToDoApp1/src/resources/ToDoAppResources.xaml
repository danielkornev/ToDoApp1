<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Style x:Key="TodoListItemTextBlockStyle" TargetType="TextBlock">
        <Style.Triggers>
            <DataTrigger Binding="{Binding DataContext}" Value="True">
                <Setter Property="TextDecorations" Value="Strikethrough"></Setter>
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="todoHeaderH1" TargetType="TextBlock">
        <Setter Property="FontSize" Value="32" />
        <Setter Property="FontWeight" Value="Thin" />
    </Style>
</ResourceDictionary>